{
  "name": "lotus-test",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev:up": "docker compose -f docker-compose.dev.yml --env-file .env.development up -d --build --remove-orphans",
    "dev:down": "docker compose -f docker-compose.dev.yml --env-file .env.development down",
    "prod:up": "docker compose -f docker-compose.prod.yml --env-file .env.production up -d --build --remove-orphans",
    "prod:down": "docker compose -f docker-compose.prod.yml --env-file .env.production down"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}
